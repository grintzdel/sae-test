<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<div class="container mt-5">
    <h1 class="text-center">Admin</h1>

    <div class="mt-4">
        <h2>Choisissez une action :</h2>
        <ul class="list-group">
            <li class="list-group-item">
                <a href="{{ path('admin_train', { 'algorithm': 'mlp' }) }}" class="btn btn-primary">Entraîner le modèle MLP</a>
            </li>
            <li class="list-group-item">
                <a href="{{ path('admin_train', { 'algorithm': 'tree' }) }}" class="btn btn-primary">Entraîner le modèle Tree</a>
            </li>
            <li class="list-group-item">
                <a href="{{ path('admin_test', { 'algorithm': 'mlp' }) }}" class="btn btn-secondary">Tester le modèle MLP</a>
            </li>
            <li class="list-group-item">
                <a href="{{ path('admin_test', { 'algorithm': 'tree' }) }}" class="btn btn-secondary">Tester le modèle Tree</a>
            </li>
        </ul>
    </div>
</div>

</body>
</html>