<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Prédiction : Tree</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

{% block body %}
<div class="container py-5">
    <div class="position-absolute top-0 end-0 p-3 text-end">
        <h4>RandomForest & Classification Tree</h4>
        <p>envrion 79% de précision</p>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="text-center mb-4">
                <h1 class="display-4">Tester le Modèle</h1>
                <p class="text-muted">Envoyez une image pour tester le modèle de prédiction</p>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <form action="{{ path('Tree') }}" method="post" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label for="image" class="form-label">Uploader une image 28*28px (voir dossier des images)
                                :</label>
                            <input type="file" id="image" name="image" accept="image/png, image/jpeg"
                                   class="form-control" required>
                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-primary">Tester</button>
                        </div>
                    </form>
                </div>
            </div>

            {% for message in app.flashes('success') %}
                <div class="alert alert-success mt-4 text-center">{{ message }}</div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div class="alert alert-danger">{{ message }}</div>
            {% endfor %}

            <div class="text-center mt-4">

            </div>
        </div>
    </div>
</div>
{% endblock %}